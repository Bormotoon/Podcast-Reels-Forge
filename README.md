# 🎙️ Podcast Reels Forge

## Автоматическое создание Reels/Shorts из подкастов (локально)

[![Python 3.10+](https://img.shields.io/badge/python-3.10+-blue.svg)](https://www.python.org/downloads/)
[![License: MIT](https://img.shields.io/badge/License-MIT-yellow.svg)](LICENSE)

[English](README.en.md) | **Русский**

---

## 📌 Оглавление

- Что делает проект
- Быстрый старт
- Как устроен пайплайн
- Где лежат результаты
- Модели и структура `output/<model>/`
- Перегенерация видео из готового `moments.json` (без LLM)
- Smart crop по лицу
- Запуск отдельных стадий
- Конфигурация (`config.yaml`)
- Производительность и стабильность
- Troubleshooting / FAQ
- Документация

---

## 📋 Что это и что делает

**Podcast Reels Forge** — CLI-инструмент, который:
1) транскрибирует видео/аудио через `faster-whisper` (CUDA/CPU),
2) ищет «вирусные» моменты через LLM (по умолчанию Ollama),
3) режет видео на клипы через FFmpeg,
4) сохраняет результаты **отдельно для каждой модели** в `output/<model>/`.

Подробный user-guide: [docs/USER_GUIDE.md](docs/USER_GUIDE.md)

---

## ✨ Ключевые особенности

- **5 поддерживаемых моделей** (qwen3/deepseek/gemma3/gemma2/gemini3) и раздельные выходы.
- **Однопроходный анализ**: без “разгоняющих” промптов и без многократных repair/selection итераций.
- **Устойчивость**: падение/зависание одной модели не ломает остальные.
- **Watchdog для Ollama**: таймаут первого токена/застоя + per-model overrides.
- **Вертикальная нарезка 9:16** (в пайплайне) и **гарантированный re-render** (скрипт).
- **Face-aware smart crop (опционально)**: вертикальный кроп смещается к лицу.

---

## 🚀 Быстрый старт

### Требования

- Python 3.10+
- FFmpeg: `ffmpeg` и `ffprobe` должны быть доступны в PATH
- Ollama (если используете локальные LLM)

Проверка:

```bash
ffmpeg -version
ffprobe -version
```

### Установка

```bash
python3 -m venv whisper-env
source whisper-env/bin/activate
pip install -r requirements.txt
```

### Подготовка входных данных

Положите видео/аудио в `input/`. Пайплайн возьмёт **самый новый** файл.

Пример:

```bash
cp /path/to/video.mp4 input/video.mp4
```

### Запуск полного пайплайна

```bash
python3 start_forge.py
```

Полезные флаги:
- `--verbose` — подробные логи
- `--quiet` — только ошибки
- `--no-skip-existing` — пересчитать стадии даже если выходы уже есть

---

## 🔩 Как устроен пайплайн

Пайплайн оркестрируется из [start_forge.py](start_forge.py) → [podcast_reels_forge/pipeline.py](podcast_reels_forge/pipeline.py):

1) **Transcribe**: создаёт `output/<input_name>.json` (транскрипт с `segments`).
2) **Analyze**: для каждой модели создаёт `output/<model>/moments.json` и `output/<model>/reels.md`.
3) **Cut**: режет видео на `output/<model>/reels/reel_XX.mp4` и собирает `reels_preview.mp4`.

Важно:
- Анализ сейчас ожидает **валидный JSON** от LLM (без “починки” вторым вызовом).
- Финального LLM-selection шага **по умолчанию нет** (выбор лучших моментов делается локально по `score`).

---

## 📁 Где лежат результаты

### Транскрипт

- `output/<имя_входного_файла>.json` — результат транскрибации.

### Выходы по моделям

Для каждой модели (например `output/gemma3/`):

- `moments.json` — найденные моменты (start/end/title/quote/why/score/...)
- `reels.md` — текстовые заготовки/описания
- `reels/` — нарезанные клипы `reel_01.mp4`, `reel_02.mp4`, ...
- `reels_preview.mp4` — конкат-превью всех клипов

Имена папок моделей фиксированы и короткие:
- `qwen3`, `deepseek`, `gemma3`, `gemma2`, `gemini3`

---

## 🎞️ Перегенерация видео из готового moments.json (без LLM)

Это лучший способ исправить качество/формат без повторного анализа.

Скрипт-лаунчер в корне: [rerender_videos.py](rerender_videos.py)

Примеры:

```bash
# Перерендер для одной модели
python3 rerender_videos.py --model gemma3

# Для всех моделей, которые есть в output/
python3 rerender_videos.py
```

Что он делает по умолчанию:
- берёт `output/<model>/moments.json`
- пишет в `output/<model>/reels_rerendered/`
- **не перезаписывает**: при совпадении имён создаёт `reel_01_2.mp4`, `reel_01_3.mp4`, ...

Параметры экспорта по умолчанию:
- 1080x1920
- 30fps
- H.264 (`libx264`)
- AAC
- `-b:v 5000k`

Чтобы перезаписать клипы в `reels/`:

```bash
python3 rerender_videos.py --model gemma3 --replace
```

---

## 🙂 Smart crop по лицу (опционально)

Иногда центр-кроп режет говорящего. Можно включить детекцию лица и смещать окно 9:16.

```bash
python3 rerender_videos.py --model gemma3 --smart-crop-face
```

Опции:
- `--face-samples 7` — сколько кадров семплить на клип
- `--face-min-size 60` — минимальный размер лица для детекта

Примечания:
- Используется OpenCV Haar cascade (быстро, оффлайн).
- Если лицо не найдено — автоматически откатывается к обычному центр-кропу.

---

## 🧩 Запуск отдельных стадий

### Только транскрибация

```bash
python -m podcast_reels_forge.scripts.transcribe \
	--input input/video.mp4 \
	--outdir output \
	--model medium \
	--language ru
```

### Только анализ (для одной модели)

```bash
python -m podcast_reels_forge.scripts.analyze \
	--transcript output/video.json \
	--outdir output/gemma2 \
	--provider ollama \
	--model gemma2:9b \
	--timeout 600 \
	--chunk-seconds 600
```

### Только нарезка видео (пайплайн-резак)

```bash
python -m podcast_reels_forge.scripts.video_processor \
	--input input/video.mp4 \
	--moments output/gemma3/moments.json \
	--outdir output/gemma3 \
	--vertical \
	--padding 5
```

### A/B тестирование промптов

```bash
python -m podcast_reels_forge.scripts.evaluate_prompts \
	--transcript output/video.json \
	--outdir output \
	--variants default,a,b \
	--provider ollama \
	--model gemma2:9b
```

---

## ⚙️ Конфигурация (config.yaml)

Файл: [config.yaml](config.yaml)

Самое важное:

### `ollama.models` и per-model overrides

- Пайплайн запускает анализ для всех моделей из `ollama.models`.
- Для медленных моделей можно задавать overrides в `ollama.model_overrides`.

### Watchdog

Если модель долго молчит до первого токена — увеличьте:
- `ollama.watchdog.first_token_timeout`

Если модель «подвисает» в процессе стриминга — увеличьте:
- `ollama.watchdog.stall_timeout`

---

## ⚡ Производительность и стабильность

- Если всё слишком медленно: увеличивайте `ollama.chunk_seconds` (меньше запросов), но следите за качеством.
- Если qwen3/deepseek долго прогреваются: используйте `ollama.model_overrides` с большим `first_token_timeout`.
- Если Whisper падает по CUDA OOM: уменьшайте `transcription.model` или ставьте `transcription.device: cpu`.

---

## ❓ Troubleshooting / FAQ

### Видео получилось горизонтальным

- Для пайплайна проверьте `video.vertical_crop: true` в [config.yaml](config.yaml).
- Для гарантированного 9:16 и стабильных параметров используйте `python3 rerender_videos.py`.

### LLM вернул невалидный JSON → moments пустой

Сейчас пайплайн **не делает** повторный “repair” вызов.
Решения:
- попробуйте другую модель из списка,
- уменьшите размер чанка (`ollama.chunk_seconds`),
- отредактируйте промпт (см. [docs/PROMPTS.md](docs/PROMPTS.md)).

### FFmpeg не найден

Установите `ffmpeg` в систему (Ubuntu: `sudo apt install ffmpeg`).

---

## 📚 Документация

- [docs/USER_GUIDE.md](docs/USER_GUIDE.md) — руководство пользователя
- [docs/CONFIGURATION.md](docs/CONFIGURATION.md) — конфигурация
- [docs/PROMPTS.md](docs/PROMPTS.md) — промпты
- [docs/DEVELOPMENT.md](docs/DEVELOPMENT.md) — разработка

---

## 📄 Лицензия

MIT License — см. [LICENSE](LICENSE).
