# 🎙️ Podcast Reels Forge


## Автоматическое создание вирусных клипов (Reels/Shorts) из подкастов

## Automatically create viral clips (Reels/Shorts) from podcasts

[![Python 3.10+](https://img.shields.io/badge/python-3.10+-blue.svg)](https://www.python.org/downloads/)
[![License: MIT](https://img.shields.io/badge/License-MIT-yellow.svg)](LICENSE)
[![Code style: black](https://img.shields.io/badge/code%20style-black-000000.svg)](https://github.com/psf/black)

[English](README.en.md) | **Русский**

---

## 📋 Описание

**Podcast Reels Forge** — это инструмент командной строки для автоматического создания коротких вирусных видео (Reels, Shorts, TikTok) из длинных подкастов и интервью.

### 🎯 Что делает этот инструмент

1. **Транскрибирует** аудио/видео с помощью [faster-whisper](https://github.com/SYSTRAN/faster-whisper) (локально, GPU/CPU)
2. **Анализирует** транскрипт с помощью LLM (Ollama) для поиска самых интересных моментов
3. **Нарезает** видео на готовые клипы с автоматическим кропом в вертикальный формат 9:16
4. **Экспортирует** в различные форматы (MP4, WebM, GIF, аудио)

### ✨ Ключевые особенности

- 🚀 **Одна команда** — весь пайплайн запускается одной командой
- 🎛️ **Гибкая конфигурация** — все параметры в YAML-файле
- 🧠 **Мульти-модельный анализ** — прогоняет несколько LLM и сохраняет результаты по папкам моделей
- 🌍 **Мультиязычность** — промпты на русском и английском, автоопределение языка
- 📊 **A/B тестирование** — встроенная оценка качества промптов
- 🎬 **Вертикальный формат** — автоматический кроп 16:9 → 9:16 для социальных сетей
- 🔄 **Параллельная обработка** — многопоточная нарезка видео через FFmpeg

---

## 🚀 Быстрый старт

### Требования

- Python 3.10+
- FFmpeg (установлен в системе)
- CUDA (опционально, для GPU-ускорения)
- Ollama (опционально, для локального LLM)

### Установка

```bash
# Клонировать репозиторий
git clone https://github.com/yourusername/podcast-reels-forge.git
cd podcast-reels-forge

# Создать виртуальное окружение
python3 -m venv whisper-env
source whisper-env/bin/activate

# Установить зависимости
pip install -r requirements.txt
```

### Использование

```bash
# 1. Положите видео в папку input/
cp your_podcast.mp4 input/

# 2. Запустите пайплайн
python3 start_forge.py

# 3. Результаты будут в папке output/ (по подпапкам моделей)
#    - output/<model>/reels/       — готовые клипы
#    - output/<model>/moments.json — метаданные моментов
#    - output/<model>/reels.md     — описания для публикации
```

---

## 📁 Структура проекта

```text
podcast-reels-forge/
├── start_forge.py              # Главная точка входа
├── config.yaml                 # Конфигурация пайплайна
├── requirements.txt            # Зависимости
├── input/                      # Входные видео/аудио файлы
├── output/                     # Результаты обработки
│   ├── qwen3/                  # Результаты модели qwen3
│   ├── deepseek/               # Результаты модели deepseek
│   ├── gemma3/                 # Результаты модели gemma3
│   ├── gemma2/                 # Результаты модели gemma2
│   └── gemini3/                # Результаты модели gemini3
├── prompts/                    # LLM промпты
│   ├── ru/                     # Русские промпты
│   └── en/                     # Английские промпты
├── podcast_reels_forge/        # Основной пакет
│   ├── pipeline.py             # Оркестрация пайплайна
│   ├── scripts/                # CLI скрипты этапов
│   │   ├── analyze.py          # Анализ с LLM
│   │   ├── transcribe.py       # Транскрипция
│   │   ├── video_processor.py  # Обработка видео
│   │   ├── diarize.py          # Диаризация (опционально)
│   │   └── evaluate_prompts.py # A/B тестирование промптов
│   ├── llm/                    # LLM провайдеры
│   │   └── providers.py        # Ollama, OpenAI, Anthropic, Gemini
│   ├── stages/                 # Модули этапов
│   └── utils/                  # Утилиты
└── tests/                      # Тесты
```

---

## ⚙️ Конфигурация

Все настройки находятся в файле `config.yaml`:

```yaml
# Пути к директориям
paths:
  input_dir: "input"
  output_dir: "output"

# Настройки CLI
cli:
  quiet: false      # Только ошибки
  verbose: false    # Подробный вывод

# Транскрипция (faster-whisper)
transcription:
  model: "small"              # tiny, base, small, medium, large-v3
  device: "cuda"              # cuda или cpu
  language: "ru"              # ru, en или auto
  beam_size: 5
  compute_type: "int8_float16"

# Настройки Ollama (локальные модели)
ollama:
  url: "http://127.0.0.1:11434/api/generate"
  # Будут прогнаны все модели по очереди, результат — в output/<model>/
  models:
    - "qwen3:latest"
    - "deepseek-r1:8b"
    - "gemma3:4b"
    - "gemma2:9b"
    - "gemini-3-flash-preview:latest"
  timeout: 900
  temperature: 0.3

# Промпты
prompts:
  language: "ru"              # ru | en | auto
  variant: "default"          # default | a | b (A/B тестирование)

# Параметры обработки
processing:
  reels_count: 4              # Количество клипов
  reel_min_duration: 30       # Мин. длина клипа (сек)
  reel_max_duration: 60       # Макс. длина клипа (сек)
  reel_padding: 5             # Отступ от границ момента (сек)

# Экспорт дополнительных форматов
exports:
  webm: false
  gif: false
  audio_only: false

# Настройки видео
video:
  threads: 4
  vertical_crop: true         # Кроп в 9:16
  video_bitrate: "5M"
  audio_bitrate: "192k"
  preset: "fast"

# Диаризация (опционально)
diarization:
  enabled: false
  model: "pyannote/speaker-diarization"
```

---

## 🤖 LLM (Ollama)

### Ollama (локально)

```bash
# Установить Ollama
curl -fsSL https://ollama.com/install.sh | sh

# Загрузить модели
ollama pull qwen3:latest
ollama pull deepseek-r1:8b
ollama pull gemma3:4b
ollama pull gemma2:9b
ollama pull gemini-3-flash-preview:latest

# Конфигурация
ollama:
  models:
    - "qwen3:latest"
    - "deepseek-r1:8b"
    - "gemma3:4b"
    - "gemma2:9b"
    - "gemini-3-flash-preview:latest"
```

---

## 📊 A/B тестирование промптов

Инструмент включает систему для сравнения качества разных промптов:

```bash
# Запустить оценку всех вариантов
python -m podcast_reels_forge.scripts.evaluate_prompts \
    --transcript output/transcript.json \
    --outdir output \
    --variants default,a,b

# Результат в output/prompt_eval.json
```

Файлы промптов находятся в `prompts/ru/` и `prompts/en/`:

- `chunk_default.txt`, `chunk_a.txt`, `chunk_b.txt` — промпты для анализа чанков
- `select_default.txt`, `select_a.txt`, `select_b.txt` — промпты для финального выбора

---

## 🔧 CLI опции

```bash
python3 start_forge.py --help

# Опции:
#   --config CONFIG   Путь к config.yaml (по умолчанию: config.yaml)
#   --quiet           Только ошибки
#   --verbose         Подробный вывод
```

### Запуск отдельных этапов

```bash
# Только транскрипция
python -m podcast_reels_forge.scripts.transcribe \
    --input input/video.mp4 \
    --outdir output \
    --model medium \
    --language ru

# Только анализ
python -m podcast_reels_forge.scripts.analyze \
    --transcript output/video.json \
    --outdir output \
    --provider ollama \
    --model gemma2:9b

# Только нарезка видео
python -m podcast_reels_forge.scripts.video_processor \
    --input input/video.mp4 \
    --moments output/moments.json \
    --outdir output \
    --vertical
```

---

## 🧪 Разработка

```bash
# Установить зависимости для разработки
pip install -r requirements-dev.txt

# Запустить тесты
pytest

# Проверка типов
mypy podcast_reels_forge/

# Форматирование кода
black podcast_reels_forge/ tests/
```

---

## 📦 Выходные файлы

После выполнения пайплайна в папке `output/` будут:

| Файл | Описание |
| ------ | ---------- |
| `{video}.json` | Транскрипт с таймкодами |
| `moments.json` | Найденные виральные моменты |
| `reels.md` | Markdown с описаниями для публикации |
| `reels/reel_01.mp4` | Готовые клипы в вертикальном формате |
| `reels_preview.mp4` | Превью всех клипов подряд |
| `diarization.json` | Диаризация (если включена) |

---

## ❓ FAQ

### Как выбрать модель Whisper?

| Модель | VRAM | Точность | Скорость |
| ------ | ------ | ---------- | ---------- |
| `tiny` | ~1GB | Низкая | Очень быстро |
| `base` | ~1GB | Ниже среднего | Быстро |
| `small` | ~2GB | Средняя | Средне |
| `medium` | ~5GB | Хорошая | Медленно |
| `large-v3` | ~10GB | Лучшая | Очень медленно |

### Не работает NVENC

Если видеокарта не поддерживает NVENC, пайплайн автоматически переключится на `libx264`.

### LLM выдаёт некорректный JSON

Пайплайн автоматически пытается "починить" JSON через повторный запрос к LLM.

---

## 📄 Лицензия

MIT License — см. файл [LICENSE](LICENSE).

---

## 🙏 Благодарности

- [faster-whisper](https://github.com/SYSTRAN/faster-whisper) — быстрая транскрипция
- [Ollama](https://ollama.com/) — локальные LLM
- [FFmpeg](https://ffmpeg.org/) — обработка видео
- [pyannote-audio](https://github.com/pyannote/pyannote-audio) — диаризация спикеров
