# Podcast Reels Forge Configuration
#
# Default workflow:
# - Python 3.10+
# - Local llama.cpp only for the main analysis path
# - Gemma 4 lineup
# - FFmpeg + pycaps for rendering

paths:
  input_dir: "input"
  output_dir: "output"

cli:
  quiet: false
  verbose: false

cache:
  enabled: true
  validate_json: true

transcription:
  model: "large-v3"
  device: "auto"          # auto | cuda | cpu
  language: "auto"        # auto | ru | en
  beam_size: 5
  compute_type: "auto"    # auto | float32 | float16 | int8 | int8_float16 | int8_float32
  best_of: 1
  patience: 1.0
  # Batched GPU inference: main speed lever. Transcription runs alone (llama.cpp starts
  # after), so all 16GB is free: 16 fits large-v3 in ~10-11GB. OOM auto-halves the batch.
  batch_size: 16
  # Anti-hallucination: stops the endless "Спасибо." loop on silence/music.
  condition_on_previous_text: false
  repetition_penalty: 1.1
  no_repeat_ngram_size: 3
  # fast = batched pipeline (~5 min/episode, no cross-segment context).
  # quality = sequential pipeline with context + hallucination guard: more accurate on
  # hard/quiet/far-field audio (fixes word-agreement errors), but ~10x slower (~1h/episode).
  mode: "fast"
  quality_beam_size: 10        # beam used only in quality mode
  # Optional domain context to bias vocabulary (helps both modes). Example:
  # initial_prompt: "Родительское собрание в школе. Программа, занятия, преподаватели."
  initial_prompt: ""

llama_cpp:
  url: "http://127.0.0.1:11440/completion"
  service:
    auto_start: true
    model_path: "/opt/llamacpp/models/gemma4-26b.gguf"
    startup_timeout: 120
    # RTX 5060 Ti 16GB: gemma4:26b (Q4_K_M, ~17GB) — weights are ~15.5GB on GPU.
    # n_gpu_layers=0 → llama-server auto-fit fills VRAM maximally without OOM.
    # Non-zero values disable auto-fit; use 999 to try full offload (may OOM).
    n_gpu_layers: 0
    ctx_size: 8192
    batch_size: 1024
    ubatch_size: 512
    threads: 4
    main_gpu: 0
    parallel: 1
    # Quantize the KV cache (needs flash-attn) to ~halve its VRAM; frees headroom on 16GB.
    cache_type_k: "q8_0"
    cache_type_v: "q8_0"
    extra_args: []
  scout_parallelism: 1
  roles:
    scout: "gemma4:26b"
    cleanup: "gemma4:26b"
    refine: "gemma4:26b"
    judge: "gemma4:26b"
    metadata: "gemma4:26b"
  timeout: 600
  temperature: 0.2
  chunk_seconds: 900
  # Russian text ~2-3 chars/token. 7000 chars ≈ 2800-3500 tokens.
  # With instructions (~350 tokens) + n_predict (2048) = ~6250 total < ctx 8192.
  max_chars_chunk: 7000
  watchdog:
    enabled: true
    first_token_timeout: 90
    stall_timeout: 120
    log_interval: 10
    max_retries: 4
  fallback_models: []

  # Role-specific overrides. Keep the default flow local, predictable, and fast.
  role_overrides:
    scout:
      timeout: 600
      chunk_seconds: 1200
      temperature: 0.35
    cleanup:
      timeout: 420
      temperature: 0.12
    refine:
      timeout: 600
      temperature: 0.15
    judge:
      timeout: 600
      temperature: 0.05
    metadata:
      timeout: 420
      temperature: 0.10

  # Legacy compatibility only. Prefer role_overrides above.
  model_overrides: {}

prompts:
  language: "auto"        # auto | ru | en
  variant: "default"      # default | a | b

processing:
  quality_filters:
    min_score: 7
    min_duration: 15
    max_duration: 180
    face_min_ratio: 0.3

  clips:
    stories:
      count: 2
      max_duration: 15
    reels:
      count: 3
      max_duration: 60
    long_reels:
      count: 1
      max_duration: 180
    highlights:
      count: 1
      moments_count: 5

  reels_count: 3
  reel_min_duration: 30
  reel_max_duration: 60
  reel_padding: 5

exports:
  webm: false
  gif: false
  audio_only: false

subtitles:
  enabled: false
  font: "assets/fonts/bignoodletoooblique.ttf"
  css: "assets/subtitles/forge_subtitles.css"
  font_size_px: 46
  wrap_words: true
  max_lines: 2
  max_width_ratio: 0.92
  vertical_align: "bottom"
  vertical_offset: 0.0

video:
  threads: 4
  vertical_crop: true
  smart_crop_face: true
  video_bitrate: "8M"      # NVENC: peak ceiling for quality-driven VBR; software: target
  audio_bitrate: "192k"
  preset: "fast"           # libx264 preset (software fallback only)
  use_nvenc: true          # prefer GPU NVENC; auto-falls back to libx264 if unavailable
  nvenc_cq: 21             # NVENC VBR quality target, lower = better (18-24 sensible)
  nvenc_preset: "p5"       # NVENC preset p1(fastest)..p7(best); p5 is balanced
  face_samples: 9
  face_min_size: 72

diarization:
  enabled: false
  model: "pyannote/speaker-diarization"
